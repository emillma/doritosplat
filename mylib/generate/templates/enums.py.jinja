from ..c_bindings import c_optix

{% for e in enums %}
class {{ e.name }}:
    {% for v in e.values %}
    {{ v.name }} = c_optix.{{ e.name }}.{{ v.name }}
    {% endfor %}
{% endfor %}

{% for e in enums %}
{% for v in e.values %}
{{ v.name }} = {{e.name}}.{{ v.name }}
{% endfor %}
{% endfor %}